# Repository Guidance

## CodeGraph

This repository is indexed by CodeGraph. Use `codegraph explore` before text
search or broad file reads when locating symbols, callers, or tests. Refresh the
index after source changes when current call-path evidence is needed.
